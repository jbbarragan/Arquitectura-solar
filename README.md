# Solar·Arch — Análisis Bioclimático

<<<<<<< HEAD
Herramienta web para análisis solar y bioclimático: diagrama solar interactivo, visor 3D de modelos arquitectónicos y visualización de datos climáticos desde archivos EPW.

---

## Stack

| Capa | Tecnología |
|------|-----------|
| Backend | Python 3.11+, Flask 3, Matplotlib, NumPy, Pandas |
| Frontend | HTML5, Three.js r128, Leaflet.js 1.9, PapaParse |
| Producción | Gunicorn, Linux (Ubuntu 22.04 / Amazon Linux 2) |

---

## Instalación en AWS / Linux

```bash
# 1. Clonar / copiar el proyecto
cd /home/ubuntu/solar_arch

# 2. Crear entorno virtual
python3 -m venv venv
source venv/bin/activate

# 3. Instalar dependencias (sin basemap, sin X11)
pip install -r requirements.txt

# 4. Crear carpetas necesarias
mkdir -p uploads static

# 5. Iniciar en producción
bash start.sh 5000
```

Para exponer detrás de **nginx** agrega un proxy pass a `http://127.0.0.1:5000`.

---

## Uso

### Diagrama solar
1. Abre la aplicación en el navegador.
2. Haz clic en cualquier punto del mapa o busca una dirección.
3. El diagrama solar polar se genera automáticamente para esa latitud.

### Modelo 3D
- Arrastra un archivo `.obj`, `.mtl` o `.stl` al visor, o usa el botón **Modelo 3D**.
- El modelo se coloca automáticamente **sobre el plano/tabla**.
- Ajusta la **orientación norte** con el slider o los botones +90° / −90°.
- Usa las vistas rápidas: **Top**, **Front**, **Iso**.

### Archivo EPW
1. Haz clic en **Cargar EPW** y selecciona un archivo `.epw`.
2. Se generan automáticamente:
   - **Mapa de calor** hora × mes con escala continua de temperatura.
   - **Tabla de confort térmico** por mes (frío / neutro / cálido).
   - **Esfera solar 3D** coloreada según confort.
3. Descarga las imágenes y el CSV con los botones de la franja inferior.

---

## Estructura del proyecto

```
solar_arch/
├── app.py              # Flask principal + cálculo solar
├── lectorEPW.py        # Blueprint EPW + generación de gráficas
├── wsgi.py             # Entry point gunicorn
├── start.sh            # Script de inicio producción
├── requirements.txt
├── templates/
│   └── index.html      # SPA principal
├── static/             # Imágenes generadas (diagrama solar)
└── uploads/            # Archivos EPW, CSV y tablas generadas
```
=======
This is a system that allows us to visualize advanced solar graphs and perform thermal comfort analysis based on EPW files. The system is currently hosted at https://arquitecturasolar.duckdns.org/ and implements the innovation of analyzing temperatures across different dates of the year, allowing users to view a history of maximum and minimum values. This enables us to model optimal lighting and temperature optimizations, with the future goal of simulating intelligent structures.

<img width="1600" height="865" alt="image" src="https://github.com/user-attachments/assets/ae35d6d9-91ca-4422-afb9-526d0a016ff2" />

---

## Overview

Solar·Arch is a web-based tool for **solar and bioclimatic analysis**.  
It integrates solar geometry visualization, 3D architectural model viewing, and climatic data analysis using **EPW weather files**.

The system generates visualizations and datasets that allow you to:

- Visualize the **3D solar chart** together with your architectural model
- Analyze **temperature data extracted from EPW files**
- Evaluate **thermal comfort conditions** using adaptive comfort models
- Explore **historical temperature ranges (min/max)** across different dates of the year

The platform uses **dry bulb temperature** as a reference and applies the **adaptive comfort temperature equation** to evaluate comfort conditions.

---

## Temperature Visualization

Based on the thermal comfort reference, the indicators change **color or temperature category every 2 to 2.5 °C**, allowing intuitive interpretation of thermal conditions.

The solar chart is generated in **both 2D and 3D**, using projections and a **bimonthly temperature averaging method** to generate color samples in the 3D diagram.

The resulting solar diagram is composed of:

- **6 × 24 panels**
- Representing **average hourly temperatures**
- For **each of the 12 months**

---

## Results

Example visualizations generated by the system:

<img width="1803" height="1024" alt="image" src="https://github.com/user-attachments/assets/b95820a0-0ae9-4492-9157-c3d1809b3030" />

<img width="2067" height="1323" alt="image" src="https://github.com/user-attachments/assets/318db12d-877a-4862-a83b-5eceee64e73a" />

<img width="1039" height="974" alt="image" src="https://github.com/user-attachments/assets/c5a0ba26-c26f-49a5-b875-782f7d42f669" />

<img width="1417" height="796" alt="image" src="https://github.com/user-attachments/assets/f5828768-5731-49c3-8538-25260e1751bc" />



---

# Stack

| Layer | Technology |
|------|-----------|
| Backend | Python 3.11+, Flask 3, Matplotlib, NumPy, Pandas |
| Frontend | HTML5, Three.js r128, Leaflet.js 1.9, PapaParse |
| Production | Gunicorn, Linux (Ubuntu 22.04 / Amazon Linux 2) |

---

# Installation (AWS / Linux)

```bash
# 1. Clone or copy the project
cd /home/ubuntu/solar_arch

# 2. Create virtual environment
python3 -m venv venv
source venv/bin/activate

# 3. Install dependencies
pip install -r requirements.txt

# 4. Create required folders
mkdir -p uploads static

# 5. Start in production
bash start.sh 5000


To expose the service behind nginx, configure a proxy pass to:
http://127.0.0.1:5000

Usage
Solar Diagram

Open the application in your browser.

Click anywhere on the map or search for an address.

The polar solar diagram is automatically generated for that latitude.

3D Architectural Model

Drag a .obj, .mtl, or .stl file into the viewer, or use the 3D Model button.

The model is automatically positioned on top of the base plane/table.

Adjust the north orientation using the slider or the +90° / −90° buttons.

Use quick views: Top, Front, Iso.

EPW Weather File

Click Upload EPW and select an .epw file.

The system automatically generates:

Temperature heatmap (hour × month)

Thermal comfort table by month (cold / neutral / warm)

3D solar sphere colored by comfort

Download generated images and CSV files using the buttons in the lower toolbar.

Project Structure


solar_arch/
├── app.py              # Main Flask app + solar calculations
├── lectorEPW.py        # EPW processing + graph generation
├── wsgi.py             # Gunicorn entry point
├── start.sh            # Production startup script
├── requirements.txt
├── templates/
│   └── index.html      # Main SPA interface
├── static/             # Generated solar diagrams
└── uploads/            # EPW, CSV and generated tables
>>>>>>> 59da878b1171ddf1704a46f519fd727fd7dc6e9e
